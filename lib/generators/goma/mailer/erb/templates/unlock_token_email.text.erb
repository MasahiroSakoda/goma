Hello, <%%= @<%= resource_name %>.<%= goma_config.email_attribute_name %> %>
============================================================

Your account has been locked due to an excessive number of unsuccessful login attempts.

Click the link below to unlock your account:


<%%= unlock_url(@<%= resource_name %>.<%= goma_config.unlock_token_to_send_attribute_name %>) %>

